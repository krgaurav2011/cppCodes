#include <stdio.h>

struct lnode
{
	int data;
	struct lnode* next;
};

struct tnode
{
	int data;
	struct tnode* left;
	struct tnode* right;
};

struct lnode* newnode(int x)
{
	struct lnode * n = (struct node*)malloc(sizeof(struct node));

}
